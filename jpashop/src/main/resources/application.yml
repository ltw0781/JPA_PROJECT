spring:
  datasource:
    url: jdbc:mysql://localhost:3306/jpashop?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: jpashop
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create # 필요시 update로 변경
    properties:
      hibernate:
        # format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect  # ✅ 명시적으로 지정
    show-sql: true

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.orm.jdbc.bind: trace
